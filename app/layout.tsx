import "./globals.css";

export const metadata = {
  title: "SPM Green Tech & Developers",
  description: "SPM Green Tech & Developers",
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}